name: Run Gradle on PRs
on:
  push:
    branches:
      - main
jobs:
  gradle:
    #    strategy:
    #      matrix:
    #        os: [ubuntu-latest, macos-latest, windows-latest]
    #    runs-on: ${{ matrix.os }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 7.5.1

      - name: Execute Gradle build
        run: ./gradlew packageDistributionForCurrentOS

      - run: pwd
      - run: ls -R
      - name: Upload APK
        uses: actions/upload-artifact@v2
        with:
          name: app
          path: build/compose/binaries/main/deb/passwddesktop_1.0.0-1_amd64.deb



  # release-package:
  #   permissions: write-all
  #   name: Release Package
  #   needs: gradle
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: pwd
  #     - run: ls -R
  #     - name: Upload APK
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: app
  #         path: build/compose/binaries/main/deb/passwddesktop_1.0.0-1_amd64.deb

#      - name: Create Release
#        id: create_release
#        uses: actions/create-release@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.PUBLISH_PACKAGE }}
#        with:
#          tag_name: ${{ github.run_number }}
#          release_name: Release ${{ github.ref }}
#
#      - name: Upload Release Package
#        id: upload_release_asset
#        uses: actions/upload-release-asset@v1.0.1
#        env:
#          GITHUB_TOKEN: ${{ secrets.PUBLISH_PACKAGE }}
#        with:
#          upload_url: ${{ steps.create_release.outputs.upload_url }}
#          asset_path: build/compose/binaries/main/deb/passwddesktop_1.0.0-1_amd64.deb
#          asset_name: ${{ github.event.repository.name }}  ${{ steps.branch-name.outputs.current_branch }} v${{ github.run_number }}.${{ github.run_attempt }}.deb
#          asset_content_type: application/zip
